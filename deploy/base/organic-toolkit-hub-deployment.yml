apiVersion: apps/v1
kind: Deployment
metadata:
  name: organic-toolkit-hub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: organic-toolkit-hub
  template:
    metadata:
      labels:
        app: organic-toolkit-hub
      annotations:
        elastic.co/dataset: kubernetes.container_logs.organic_toolkit_hub
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - organic-toolkit-hub
                topologyKey: "kubernetes.io/hostname"
      containers:
        - name: organic-toolkit-hub
          image: organic-toolkit-hub
          volumeMounts:
            - mountPath: /tmp
              name: uploads
          ports:
            - containerPort: 8000
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi" # this value may need to increase if application stability declines
              cpu: "1000m"
      imagePullSecrets:
        - name: github-psdi-registry-credentials
      volumes:
        - name: uploads
          emptyDir:
            sizeLimit: 1Gi
